# Vaultwarden Configuration

# Domain configuration
DOMAIN=https://localhost:9010

# Disable registration (set to false to allow new user registrations)
SIGNUPS_ALLOWED=true

# Disable invitation (set to false to allow invitations)
INVITATIONS_ALLOWED=true

# Email settings (optional - uncomment and configure if needed)
# SMTP_HOST=smtp.gmail.com
# SMTP_FROM=no-reply@yourdomain.com
# SMTP_FROM_NAME=Vaultwarden
# SMTP_PORT=587
# SMTP_SSL=true
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Admin token (generate a strong random token)
# Uncomment and set a strong password to enable admin panel at /admin
# ADMIN_TOKEN=your-super-secure-admin-token-here

# Web vault enabled
WEB_VAULT_ENABLED=true

# Log level (trace, debug, info, warn, error, off)
LOG_LEVEL=info

# Websocket configuration
WEBSOCKET_ENABLED=true
WEBSOCKET_ADDRESS=0.0.0.0
WEBSOCKET_PORT=3012

# Database URL (using PostgreSQL)
DATABASE_URL=postgresql://vaultwarden_db_user:S8cSPqDsixgjewRHmB4WQ6Car1KkBmHXdycdeoXrmHY0fOiC@db:5432/vaultwarden_db

# Rocket configuration
ROCKET_ADDRESS=0.0.0.0
ROCKET_PORT=80

# Security settings
PASSWORD_ITERATIONS=100000

# File uploads
ATTACHMENTS_FOLDER=/data/attachments
ICON_CACHE_FOLDER=/data/icon_cache